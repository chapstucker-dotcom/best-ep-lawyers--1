# Optional: Supabase (if you want signups + upgrades persisted)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: simple admin fallback (if no Supabase). Choose a strong admin key.
NEXT_PUBLIC_ADMIN_KEY=change-me-admin-key
